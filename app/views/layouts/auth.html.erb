<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">

  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= render "shared/meta" %>

  <%= yield :head %>

  <%# PWA manifest (optional) %>
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <%= favicon_link_tag "favicon-32x32.png", rel: "icon", type: "image/png", sizes: "32x32" %>
  <%= favicon_link_tag "favicon-16x16.png", rel: "icon", type: "image/png", sizes: "16x16" %>
  <%= favicon_link_tag "apple-touch-icon.png", rel: "apple-touch-icon", sizes: "180x180" %>

  <!-- Optional modern SVG favicon -->
  <%= favicon_link_tag "logo-mvpscaler-mark.svg", rel: "icon", type: "image/svg+xml" %>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@latest/font/bootstrap-icons.css">

  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= render "layout/json_ld" %>
  <%= render "layout/analytics" %>
</head>

<body class="min-h-screen">
<section id="hero" class="relative isolate min-h-screen w-full overflow-hidden">
  <!-- FULL-BLEED BACKGROUND (same as landing) -->
  <picture>
    <source srcset="<%= image_path('hero-bg.avif') %>" type="image/avif">
    <source srcset="<%= image_path('hero-bg.webp') %>" type="image/webp">
    <img src="<%= image_path('hero-bg.png') %>" alt="" aria-hidden="true"
         width="2400" height="1400" fetchpriority="high" decoding="async"
         class="absolute inset-0 -z-20 h-full w-full object-cover">
  </picture>

  <!-- Subtle white wash -->
  <div class="absolute inset-0 -z-10 bg-white/20 mix-blend-multiply"></div>

  <!-- Centered auth card -->
  <div class="relative z-10 flex min-h-screen items-center justify-center p-6">
    <div class="w-full max-w-md rounded-2xl bg-gray-900/95 px-6 py-8 shadow-2xl ring-1 ring-black/10">
      <!-- Brand -->
      <div class="mb-6 flex items-center justify-center gap-2 text-gray-200">
        <%= image_tag "logo-mvpscaler.svg", class: "h-10" %>
        <span class="sr-only">MVPScaler</span>
      </div>

      <%= render("shared/flash") if lookup_context.exists?("shared/_flash") %>
      <%= yield %>

      <!-- Footer watermark -->
      <div class="mt-6 text-center text-xs text-gray-500">
        Made with MVPScaler <i class="bi bi-heart-fill text-emerald-500"></i>
      </div>
    </div>
  </div>
</section>
</body>
</html>