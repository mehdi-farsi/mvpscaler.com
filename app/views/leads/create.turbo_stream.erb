<%= turbo_stream.replace "cta_form" do %>
  <div id="cta_form"
       class="rounded-lg bg-emerald-600 text-white px-4 py-3 sm:px-5 sm:py-4 shadow-md"
       role="status" aria-live="polite">
    <div class="flex items-center gap-3">
      <i class="bi bi-check-circle-fill text-xl sm:text-2xl"></i>
      <div>
        <p class="font-medium">
          You’re on the list.
        </p>
        <p class="text-sm opacity-90">
          We’re finalizing the last details and will notify you as soon as early access opens.
        </p>
      </div>
    </div>
  </div>
<% end %>